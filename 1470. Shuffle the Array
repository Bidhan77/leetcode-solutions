class Solution {
public:
    vector<int> shuffle(vector<int>& nums, int n) {
        vector<int>b;
        for(int i=0;i<n;i++){
            b.push_back(nums[i]);
            b.push_back(nums[i+n]);
        }
        return b;
    }
};
